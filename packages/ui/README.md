# `@glofi/ui` (optional later)

Shared design system for web + mobile (buttons, cards, tokens).

Empty by design until branding stabilizes. Prefer building components in `apps/web` first, then extract here when mobile starts.
