# Copy to apps/web/.env.local for frontend overrides (optional).
# Never commit real keys.

# Public RPC (defaults to Amoy public endpoint in @glofi/sdk)
# NEXT_PUBLIC_RPC_URL=https://rpc-amoy.polygon.technology

# Contracts package only — deployer key for Hardhat scripts
# PRIVATE_KEY=
# AMOY_RPC_URL=https://rpc-amoy.polygon.technology
